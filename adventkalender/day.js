const fs = require("fs");
const inputFileName = ("day1.txt");
let content = fs.readFileSync(inputFileName).toString();
let depth = content.split("\n").map(1 => 1.trim())

for (let i = 1; depth.length; i++){

}